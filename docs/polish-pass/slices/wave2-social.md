# Wave 2, area social

Findings from the wave 2 audit for `/community`, `/clubs`, `/inbox`, `/friend`, `/tournaments`, `/tv` and `/leaderboard` (slice D files). Every probe ran against the shared dev server on :3000 through `scripts/polish/heavy.sh`. Other agents were editing files during the runs, so Fast Refresh reloaded pages mid-probe now and then; one CLS cell errored for that reason and is marked. Evidence is in `docs/polish-pass/evidence/wave2/social/`.

## Results

| # | Finding | Status | Before | After | Commit |
|---|---|---|---|---|---|
| 1 | /community CLS far over budget (pill, rail skeletons, Friends card, Recent opponents) | PARTIAL | `cls-before.json`, 2 runs: 360 signed-out 0 / 0.064, 1280 signed-out 0.028 / 0.032, 360 user 0 / 0.255, 1280 user 0.060 / 0.061 | `cls-community-after.json`, 3 runs: 1280 signed-out 0.0003 / 0.0025 / 0.0027. Returning signed-in visitor (session plus the `nc_who` cookie that `/api/auth/me` sets, `cls-community-returning-after.json`): 1280 0.0003 twice, 360 0.0001 and 0.166. What is left is data and harness dependent, see the notes | 410647c |
| 2 | /community rail skeleton does not match the loaded rail | DONE (same fix as 1) | rail panels moved dy -154 / dh 127 (`aside > div.rail-panel`) | no rail panel in any after finding at 1280 | 410647c |
| 3 | /friend and /lobby desktop: the connection pill row shifts | HANDED OFF (core) | 1280 signed-out 0.0216 / 0.0219, 1280 user 0.0014 / 0.0298 | not changed | none |
| 4 | Console errors for expected auth responses | DONE for /friend, HANDED OFF for /mod/house | /friend signed-out: `GET /api/friends -> 401` twice | `console-after.json`: /friend and /community, signed-out and user, no 4xx on any API call (the only new items are the sandbox's blocked `arena.nerfchess.com`) | e9a3756 |
| 5 | Signed-out prompt turns into the guest view; club "Sign in to join" becomes "Join club" | DONE | /inbox showed "Sign in to message other players." from 4.4s to 10.5s, then the search view; /inbox/polish_mod "Sign in to read your messages." then a live composer (audit trace) | `first-visit-after.json`, no cookies, 360x780: /inbox shows only the search view, /inbox/polish_mod only "No messages yet.", /clubs/sweep-club only "Join club" (one early "Sign in and join" on the board line in `first-visit-after-2.json` was fixed in the same commit) | e9a3756 |
| 6 | TV live-list loading state: aria-label on a plain div | DONE | axe `aria-prohibited-attr` on `/tv` 390x844 | `axe-after.json`: gone in all four /tv cells | e9a3756 |
| 7 | Links inside sentences told apart only by colour | DONE | axe `link-in-text-block` on /community 390 and 1280 | `axe-after.json`: gone. All in-sentence sign-in and empty-state links in D files are now underlined (community x3, inbox x2, clubs x2, tournaments x1, FriendsPanel x1) | 410647c, e9a3756 |
| 8 | Social form errors not announced | DONE | only `tournaments/page.tsx:245` had `role="alert"` | `role="alert"` on the join/withdraw error (tournament), join/leave, board post and icon save errors (club), create club, create event, DM send, and the FriendsPanel add note (`status` for success, `alert` for failure) | e9a3756 |
| 9 | PlayerSearch Recent listbox holds non-option children (A-D1) | DONE | listbox children were wrapper divs, each with the option and a Remove button | the listbox holds only `role="option"` buttons; Remove buttons are a sibling column. Probe at 390x844 with three recents: children `["option","option","option"]`, option heights 50/51/51 at y 200/250/300, Remove buttons 50/51/51 at the same y; clicking Remove drops the row | e9a3756 |
| 10 | Club icon picker: 35px targets, silent error, ASCII busy label, no guard, Remove without asking | DONE | emblem `h-10`, swatches `h-10 w-10` (35px), plain span error, "Saving...", no ref guard | 44px emblems and swatches on touch (40px / 32px behind `(pointer: fine)`), `role="alert"` error, Save uses the Button `loading` state (spinner, `aria-busy`), a ref guard in `save()`, and Remove asks once inline (Remove / Keep icon), the same shape as deleting a post (F180) | e9a3756 |

Regression check: `cls-social-routes-after.json`, /inbox, /inbox/polish_mod, /clubs, /clubs/sweep-club, /tournaments, /leaderboard and /tv at 360x780 and 1280x800, signed-out and user: 27 cells all under 0.01 (max 0.0057). The 28th cell (/inbox/polish_mod 360 signed-out) errored when Fast Refresh navigated the page mid-probe. `axe-after.json` on /community, /tv, /inbox, /clubs, /tournaments at 390x844 and 1280x800: the only rule left is `color-contrast` on `.btn-leaf`, which is the shared Button (not a D file, see hand-offs). eslint and tsc clean on the touched files.

## Notes per finding

**1 and 2. /community.** Four late states, four fixes, one principle: every list is drawn at its loaded geometry and nothing late sits above something already on screen.

- (a) The online pill. Its text now sits in one grid cell with an invisible "888 players online" sizer, so the pill keeps its width when the count replaces the loading line (now "Counting players…"). It used to shrink from "Connecting to the lobby…", which let it rejoin the h1's row on a phone and lift the grid 37px. After: the pill appears in no after finding.
- (b) Rail skeletons. `RailSkeleton` (32px rows on an 8px gap, 3/4/5 rows regardless of the list) is replaced by `ListSkeleton`, which draws each list's own margin, row height, gap and count: 5 rows of 40px for Top players and Active this week, 5 of 44px for Clubs, 4 of 44px for Tournaments. `ListReserve` keeps that height as the list body's floor, so a shorter list, the empty sentence or the error line leaves the panel the same height instead of pulling every panel below it up. Empty and error lines are centred in the reserve so an empty rail reads as an empty panel. Trade-off, stated plainly: with little data (the dev database has no rated games, one club and one event) the rail panels show blank space under the one line. In production Top players and Active this week fill all five rows. A server-rendered first page of rail data would remove the need for the floor; that is a proposal, not built (below).
- (c) Friends card. Its skeleton is now 3 rows of 44px on 1px dividers with the same floor, so an empty roster no longer collapses under Recent games. The card still enters late when the session has no `nc_who` display cookie. That happens once per browser (the first `/api/auth/me` sets the cookie), but the harness's seeded `user` session never has it, which is why the harness `user` cells still show the Friends card entering (1280 user 0.059, 360 user 0.30-0.37). With the cookie present the 1280 cell is 0.0003.
- (d) Recent opponents moved below Recent games, last in the main column. It needs the full user and then a games fetch, so it always lands seconds after everything else; above Recent games its arrival pushed that card down. At the end of the column it pushes nothing on desktop.
- Recent games skeleton: 12 two-line rows (the feed returns 12), no floor. What is left at 360 (signed-out 0.21 in two of three runs, returning 0.166 in one of two): the dev database has no finished games, so the 12-row skeleton collapses to the one-line empty state and the rail below scrolls up into the viewport. Moving into the viewport counts even though the rail was below the fold. With a populated feed the loaded list is the skeleton's height. A 5-row skeleton would trade this dev-only shift for a production one (a full feed pushing the rail out of view), so it stays at 12.

**3. /friend and /lobby pill.** `/friend` is a server redirect to `/lobby?tab=friends` (F037), so both cells measure the lobby's `StatusPill` in `src/app/lobby/page.tsx` (slice B, core). Same class as 1(a): "Connecting…" and "Live · N players" differ in width, and the row it sits in reflows. Suggested fix for core: the same stacked sizer used on /community (one grid cell holding the live label and an invisible "Live · 888 players"). Not edited here.

**4. Console 401s.** `/friend` signed-out: `FriendsPanel` fetched `/api/friends` on mount, before the header had minted the guest, got 401, and then kept the signed-out view for a guest who can in fact add friends. It now reads `useSession({ ensure: true })`, loads once per account when the session is known, and a failed mint (null) is the signed-out view with no request. `/mod/house` personas 401/403 is in `src/app/mod/*` (slice G); the same pattern (read the session, skip the call unless `role` is mod or admin) applies there.

**5. Guest mint.** Root cause in the finding is right: pages called `useSession()` without `ensure`, which returns `display: null` while the header's guest mint runs. The server accepts guests for every action these pages offer (club membership, tournament entry, DMs, creating a club or an event: each route only checks for a session). Fix in the pages, not in `SessionProvider` (slice A): the inbox list, the thread, the club page, the club directory, the tournament directory and the tournament page now pass `ensure: true` (the mint is shared, `ensureAccount` dedupes it). While the answer is unknown they draw the guest's eventual frame: the inbox draws the search box and list skeleton, the thread draws its loading bubbles with Send disabled, the club and tournament pages draw a disabled Join button so the slot does not fill in late. Only a failed mint (null) shows a sign-in prompt, so "Sign in to join" (F036) remains for that case. The thread's send got a ref guard on the way (two Enter presses in one task posted twice), and so did club join/leave and tournament join/withdraw (F179 class; the server calls are idempotent, but the second POST is wasted).

**6. TV.** `role="status"` on the loading list with an sr-only "Loading live games"; the three skeleton bars are `aria-hidden`. Sibling grep: the only other `aria-label="Loading…"` in D files is on the thread's skeleton `<ul>`, where the label is allowed (list role) and the pane already carries `aria-busy`.

**8. Error lines.** Grep of D files for `text-oxblood-glow` lines without a role found the listed seven plus the FriendsPanel add note; all eight are covered.

**9. Listbox.** The Recent dropdown is one grid with two columns. The listbox (options only) and the Remove column are both `grid-rows-subgrid` children spanning the same rows, so each option and its Remove button share a row height whatever the content (two-line rows included). Keyboard behaviour is unchanged (`aria-activedescendant` on the input, arrows move through options); the Remove buttons stay in the tab order after the options, and hovering one highlights its row.

## Hand-offs

1. Core (B), `src/app/lobby/page.tsx` `StatusPill`: finding 3, fix described above.
2. Mod (G), `src/app/mod/*` house personas page: finding 4, read the session before calling `/api/mod/house/personas`.
3. Shared `src/components/ui/Button.tsx` (`.btn-leaf`): axe `color-contrast` on /community, /tournaments, /clubs, /inbox and /tv. Pre-existing in `axe-rerun.json`; not a D file.
4. Harness (`scripts/polish/seed.ts`): the seeded storage states have `dc_session` but not `nc_who`, so every signed-in cell measures a first-after-sign-in visit. Calling `/api/auth/me` once before saving the state would make the `user` cells measure the usual returning visit (this is how `cls-community-returning-after.json` was taken).

## Proposal (not built)

P-community-ssr: render `/community`'s public rail data (top players, clubs, events, active this week, recent games) on the server and pass it in as the first state, as `/tv` already does for its shell. The skeletons, the rail floors and the remaining data-dependent shifts would all go, and the rail could size to its content again.
