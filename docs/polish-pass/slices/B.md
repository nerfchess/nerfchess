# Slice B (core routes: /, /lobby, /play, /game, /game/[id]), wave 1

Owner: B agent. Ledger updates for the integrator to fold into `LEDGER.md`.
Evidence: `docs/polish-pass/evidence/B/`.

How the before numbers were taken: a production `next build` of 2d175c9 (the
tree before any slice B commit) in a detached worktree, served by `next start`
on :3100, plus the shared dev server. The after production checks used a build
of dc01ca2 on :3200. Both builds sit in the session scratchpad, not in the repo.
Note for anyone repeating this: `scripts/dev-supervisor.sh` kills every
`next-server` process when it restarts dev, so a `next start` has to be used
straight away.

Measurement scripts written for this slice (committed as e2e specs, each fails
on the before build and passes after): `e2e/polish/hero-frame.spec.ts`,
`e2e/polish/play-shell.spec.ts`, `e2e/polish/home-stable.spec.ts`,
`e2e/polish/lobby-states.spec.ts`.

## ROW UPDATES

| Row | Status | Evidence | Commit | Note |
|---|---|---|---|---|
| F003 | DONE | `f003-hero-frame.txt` (before: demo vs replay frame y 106 vs 62 on a phone, 101 vs 74 on desktop, CTAs moved 17px; after: identical), `f003-{before,after}-390-{demo,replay}.png` | 8091641 | HeroTv renders one `HeroFrame` in all three channel states: a fixed 36px top row, the board, a bottom row fixed at 44px on a coarse pointer and 36px on a fine one. Only the rows' content changes: demo (caption top, Watch TV bottom right), rerun (the one attribution line top, move count and Watch replay bottom right), live (seats and chips as before). The live state is not mocked in the spec (it needs a socket watch) but renders through the same frame. Also: the move counter showed plies as "Move N"; it now counts full moves like the lobby. The frame's action links are full 44px targets on touch (F160 hero part). |
| F019 (/lobby, /play) | DONE | `f019-prerender-before.json` (before: /play and /lobby prerendered a 268-byte body, no header, no text), `f019-ssr-after.json` (after: header, h1 and content), `f019-f006-play-shell-spec.txt` (spec fails 3 of 5 on the before build, 5 of 5 pass on dev, 7 of 7 on the after production build), `f019-play-skeleton-swap-after.txt`, `f019-lobby-skeleton-swap-after.txt` (skeleton vs first client render: identical boxes at 360, 390, 768, 1280, 1920) | edf69e1, dc01ca2 | /play: header, title, tour note and the Play online door now sit outside the Suspense boundary (static); only the setup card (the one `useSearchParams` reader) is suspended, with `BotSetupSkeleton` built from the card's own parts (Group, Pill, TimeSlider, the disabled Start button) as the fallback and in `loading.tsx`. /lobby: `FriendGameProvider` swaps the whole screen, so the header cannot leave the boundary; the fallback is the real `SiteHeader` over `LobbySkeletonBody`, which is the lobby's first client render with the data taken out (shared `LobbyWaysIn` row, same tab labels, same counters). Since slice A's layout reads cookies, these routes render per request and no longer bail out at all; the fix holds either way. |
| F006 | DONE (tour note; mode half is slice A's, done in b1f33ae) | `f019-f006-play-shell-spec.txt` (before: door top 143 then 201, the 58px push; after: one value for a first-time and a returning visitor), `cls-before.json` (/play 0.0528 / 0.0538 on a phone) | edf69e1 | The tour note is decided before the first paint: an inline script in `PlayIntro` stamps `html[data-tour-nudge]` from localStorage while the HTML is parsed, and the note's CSS shows it only under that stamp; hydration then takes over through a layout effect (which also covers client navigations, where the inline script does not run). Dismiss removes the stamp. If slice A folds the tour flag into its cookie seed (their P-A-guest-flag), the script can go. |
| F154 | DONE | `f154-play-360-before.png` (the "Medium ~1500" label runs over its pill border at 360px, 92px of content in 85px), `f154-play-360-after.png`, overflow probe (after: no element in the card overflows; page scroll width 360) | edf69e1 | Option columns are `minmax(0, 1fr)` instead of `1fr` (a 1fr column cannot shrink below its content) and a pill's label wraps inside it (flex-wrap, gap-x-1.5, centred). On phones the strength pills read as two lines like "Plain chess" already did. |
| F004 | DONE | `f004-home-loading-vs-loaded-before.txt` / `-after.txt` (loading state vs loaded state boxes; before: feed rows 32 vs 53px, the section below moved 280px, the ways-in column changed; after: identical at 390 and 1280), `f004-home-stable-spec.txt` (spec: before 3 of 3 fail, including 2 /api/lobby requests where 1 serves; after 5 of 5 pass), `cls-before.json` (the 59/60px `ul.divide-y` push) | 51ea4e3, 45d5b7f, 8a3dd72 | Live counter on the page from the first paint with skeleton numbers, and a designed "unavailable" state. Rejoin button settled before the first paint by an inline stamp (`html[data-active-game]`), and when the archive says the game ended the slot says "See how your last game ended" in place instead of disappearing. Feed skeleton uses the feed's own rows (10, same line boxes). Card of the day skeleton uses the card's layout, and the description is clamped to exactly two lines so the box has one height whatever the day's card. One shared lobby poller in `lobbyClient.ts` (`useLobbySnapshotStatus` subscribes to a module-level loop at the fastest requested interval, in-flight polls are shared; `subscribeLobbySnapshot` for non-React callers). HeroRatings reads the name from `useSession` (see the session row). |
| F005 | PARTIAL | `cls-before.json` (lobby offenders), `lobby-states.spec.ts`, `f019-lobby-skeleton-swap-after.txt`, `f005-lobby-counts.txt`, `cls-lobby-after.json` | dc01ca2, 8a3dd72, 0debf47, 264c6c6 | Done: ?tab and ?challenge are read in the first render's state (no microtask flip); the page-level error alert above the tabs is gone (a failing poll is shown by the connection pill and each rail's own error and Retry; a failed seek join is shown inside the challenges fold as its own `joinError`); the header counters hold their width with skeleton numbers; the Online now sign-in note and the QuickMatch guest note read `useSession().display`, known at first paint; the Open challenges and TV counts hold a skeleton digit instead of an ellipsis (was a 42px widening); a registered player's mode segments hold a rating-wide skeleton (was "?" replaced 44px wider) and the lobby skeleton draws the real segments (`ModeSegmentSlots`). /lobby CLS after (--runs 2): 390 signed-out 0.0012, 1280 signed-out 0.005-0.0076, 390 user 0.0036-0.0064, 1280 user 0.0108 (before 0.0174 / 0.018-0.021 baseline). Left: the connection pill label changes width with the count (13px at 1280); "Games to watch" 210 to 106px when there are no live games (P-B-games-rail); the header chip is slice A's. |
| F007 | DONE (B part) | `f007-f029-game-id-states.txt` (before: board drops 44px on a phone and 23px on desktop when the replay replaces the connecting frame; after: identical x, y, width at 390 and 1280), `e2e/polish/game-id-states.spec.ts` | 3a0e514, fd4c99e | Slow-connect note floats over the page (fixed, the ConnectionBanner position) instead of being inserted above the board; its buttons are `Button`/`LinkButton`. The connecting state, the route `loading.tsx` and the bot game's first render share one `GameFrameSkeleton` (`src/app/game/GameSkeleton.tsx`): its meta row uses GameShell's meta-row classes with blocks built like the badge and chips, and its player rows use BoardPlayerRow's box (rem units), so the board keeps its box. `SkeletonHeader` is the real `SiteHeader` (slice A's request). Not in B: a player-path skeleton in OnlineMatch's geometry (slice J). |
| F020 | DONE | `f020-bot-game-first-paint.txt` (after: with JS off the paint is the compact header, "Loading the game…" and the board frame; with JS on the header is 45px from the first frame and the status goes Loading, Dealing the cards, the game), before: `f019-prerender-before.json` ("/game": "Dealing the cards", no header) | 3a0e514 | The bot game's Suspense fallback and its not-dealt-yet state are `GameFrameSkeleton` with the compact header, instead of three bouncing dots on an empty page. A nerf-mode game still goes from this frame to the full-screen nerf draft, which has no header by design. |
| F029 | DONE | `f007-f029-game-id-states.txt`, `e2e/polish/game-id-states.spec.ts` (unknown id: the not-found panel, still on /game/NOSUCH1 at 8s; archive 503: "Something interrupted the game" with Retry, no 404 words) | 3a0e514 | `fetchReplay` answers found / absent (404) / failed. Live watch said not_found and the archive said 404 (after the existing 2.5s second look): the shared `NotFoundPanel` with `NOT_FOUND_COPY.game`, no auto-redirect. Someone did not answer: `RouteError` with plain words, Retry and Back to lobby (the raw failure string only goes to the console). The "That game has wrapped up" screen, its 6s countdown and redirect effects are removed. The spec answers the game server socket with Playwright WebSocket routing, so the state is reachable under `next dev`. |
| F131 | DONE (GameShell); OnlineMatch part is slice J's | `game-id-states.spec.ts` waits on the heading "polish_white vs polish_black, final" | 3a0e514 | GameShell (spectator and replay) has an sr-only h1 "White vs Black, live/final". |
| F112 | PARTIAL (change made, no before/after render count) | code diff | 3a0e514 | Bot game: the board's whole visual layer (rule marks, forced-move highlight, draft zones, fx layer) is one `useMemo` keyed on `game` and `myColor`, declared above the early returns (every setGame call passes a fresh object; checked). Spectator: the visual object and the reveal lists are memoized on the draft replica. A React Profiler before/after count is owed (brief 11: no perf claim without numbers). |
| F145 (lobby) | DONE | `e2e/polish/lobby-states.spec.ts` | dc01ca2 | Roving tabindex, ArrowLeft/Right, Home/End; `aria-controls` only points at a mounted panel. |
| F160 (hero, home feed) | DONE | `hero-frame` geometry (bottom row 44px on touch), code diff | 8091641, 51ea4e3 | HeroTv action links 44px on a coarse pointer; the Latest games Retry is a 44px target on touch. The community link is slice D's. |
| F161 (game/[id]) | DONE | `grep -n shadow-xl src/app/game` (none) | 3a0e514 | `shadow-xl` removed from the two floating "Show result" buttons. `QueueButton.tsx` (which carried the other shadow) is deleted. |
| F165 (B part) | DONE | `grep -rn QueueButton src` (only historical comments remain) | b006f8e (delete), dc01ca2 (comments) | `src/components/QueueButton.tsx` deleted (no importer). Lobby comments that named it now name QuickMatch. See REQUESTS for the two guard baselines that list the deleted path. |
| F200 (lobby) | DONE | code diff | dc01ca2 | The Custom game scroll uses `motionOff() ? "auto" : "smooth"` and runs in an effect after the fold renders, replacing the 60ms timeout. |
| F026 | TODO | | | The delayed skeleton primitive (150ms show delay, minimum show time) needs a CSS delay utility for server-rendered skeletons (slice I, see REQUESTS) plus a small client hook here; E1 is waiting on it too. |
| F166 | TODO | | | Needs slice E's shared footer to take a build-stamp slot and the socials row before the home page can drop its local copy (REQUESTS). |
| F114 | NO-CHANGE (B) | | | The home and lobby only display the lobby payload's count; the number itself is slice H and G's. |
| Session swap (A's list) | DONE | `lobby-states.spec.ts` and `home-stable.spec.ts` 8 of 8 after, `quickmatch-double-tap.txt` | 8a3dd72, 82f179c | `src/app/page.tsx` (HeroRatings reads `display`, BuildVersionLabel reads `user`), `src/app/lobby/page.tsx` (no own `fetchMe`, no own `ensureAccount` mint), `src/app/lobby/QuickMatch.tsx` (`useSession`, ratings keyed on the name, a start-search in-flight guard: two taps on Find during the guest mint sent 2 queue frames before, 1 now; `e2e/polish/quickmatch-double-tap.spec.ts` fails on 8a3dd72^ and passes after). `src/app/game/page.tsx` still calls `ensureAccount` at game start (an action, not a render input). |
| SkeletonHeader | DONE | slice A `evidence/A/section4-after` (the swap), `f019-*-swap-after.txt` | 26eec08, fd4c99e | First made the stand-in match the header box (it was 50px against 48 on phones), then replaced it with the real header per slice A. |
| A-B-play-contrast (new, axe) | DONE | `play-pill-contrast.txt`, `axe-after-play.json` (the 4 pill nodes gone in dark and light at 360 and 1280), `play-shell.spec.ts` "pressed option pills clear AA" (fails 2 of 2 on HEAD's setupParts, passes after) | c0b634e | Selected option pills drew the accent on a 20% accent wash (4.17:1) with the bot Elo at 70% opacity (2.83:1); now the accent on the pill's own raised surface (4.76:1 dark, 6.75:1 light), Elo undimmed. |
| B-responsive-360 (routes) | DONE (overflow) | probe (scratch): scroll width 360 on /, /lobby, /play, the bot game, a /game/<id> replay with 17-character names and the /game/<id> not-found panel | n/a | No horizontal overflow anywhere at 360x780 touch. Sub-44px targets left: inline sentence links ("Sign in" in the lobby notes, "Take the tour"), exempt as inline text; the /game/<id> meta-row names (20px, B) and BoardPlayerRow names and MoveList rows (18 to 23px, not B) are listed under TODO and REQUESTS. |

### ROUTES

| Route | layout | loading | states | interaction | a11y | responsive | Notes |
|---|---|---|---|---|---|---|---|
| `/` | DONE (F003, F004) | DONE (F004 skeletons) | DONE (counter unavailable state, rejoin ended state) | TODO | PARTIAL (axe after: 0 in dark; light has 4 `text-mode-*Glow` nodes, a token issue, see REQUESTS) | DONE (360 and 390/1280, no overflow) | Section labels are plain bold 13px headings. |
| `/lobby` | PARTIAL (F005: connection pill width, Games to watch) | DONE (F019) | DONE (join error placement, counts) | PARTIAL (F145 tabs, Quick pairing double tap guarded; seek join already guarded) | PARTIAL (tabs; axe after: only `.btn-leaf`, the shared Button) | DONE (360, no overflow) | |
| `/play` | DONE (F006) | DONE (F019) | TODO | TODO | DONE for B (labels, pill contrast; axe after: only `.btn-leaf`) | DONE (F154, 360) | |
| `/game` (bot) | DONE (frame) | DONE (F020) | TODO | TODO | DONE (axe 0 at 360 and 1280) | DONE (360, no overflow) | CLS 0 in the baseline. |
| `/game/[id]` | DONE (F007 B part) | DONE (F007) | DONE (F029) | TODO | DONE (F131 GameShell) | DONE (overflow); PARTIAL (touch targets) | States exercised with a routed socket in `game-id-states.spec.ts`. |

Guard runs at the end of this session: test:console on /, /lobby (`console-after-home-lobby.json`) and /play (`console-after-play.json`), 1280 signed-out and user: no new signatures, only the known arena lobby request that the sandbox blocks (baseline signature a). test:cls on /, /lobby, /play (`cls-core-mid.json`, `cls-lobby-after.json`): every measured cell under its ceiling. test:emdash and test:rounded OK; test:buttons and test:case report only the stale baseline entries listed under REQUESTS. polish:axe after: `axe-after-{home,lobby,play}.json`.

## ADDITIONS

- `src/app/game/GameSkeleton.tsx`: `GameFrameSkeleton` and `BoardSkeleton`, one game frame for every place a game is still being found.
- `src/app/play/{PlayIntro,PlaySkeleton,setupParts}.tsx`, `src/app/lobby/{LobbySkeleton,LobbyWaysIn}.tsx`: the static shells and skeletons, built from the same parts as the pages.
- `src/app/lobby/ModeSegment.tsx`: the Quick pairing mode segment and `ModeSegmentSlots`, so the lobby skeleton draws the real segments.
- `e2e/polish/game-id-states.spec.ts`, `e2e/polish/quickmatch-double-tap.spec.ts`: socket-routed specs (Playwright `routeWebSocket`) that reach /game/<id> states and the queue under `next dev`, which runs no game server.
- `lobbyClient.ts`: `subscribeLobbySnapshot` and `getLobbySnapshot` (non-hook access to the one shared poll).
- Two local pre-paint stamps (`data-tour-nudge` on /play, `data-active-game` on /): inline scripts next to the element they decide, same pattern as slice A's head script but for page-local storage keys.

## PROPOSALS

- P-B-games-rail: the lobby's "Games to watch" rail could reserve the skeleton's height for its empty state too (a 3-row designed empty state instead of one sentence), which removes the last F005 shift. It changes how the empty rail looks, so it is listed, not built.

## OWNER QUESTIONS

None new.

## REQUESTS

- Integrator (`scripts/check-buttons.ts` BASELINE): replace `"src/app/play/page.tsx"` with `"src/app/play/PlayIntro.tsx"` and `"src/app/play/setupParts.tsx"`, and remove `"src/components/QueueButton.tsx"`. The Play online door and the option Pill moved out of page.tsx unchanged; QueueButton is deleted (F165). `test:buttons --strict` flags the rename until then.
- Integrator (`scripts/check-case.ts`): remove `"src/app/page.tsx"` from the baseline (its uppercase section labels and "Play" chips are gone; `test:case --strict` reports it stale) and remove `"src/components/QueueButton.tsx"` (deleted).
- Slice I (`src/app/globals.css`), F026: a skeleton show-delay utility, e.g. `.skeleton-delay { animation: skeleton-in var(--dur-2) var(--ease-out) 150ms both; } @keyframes skeleton-in { from { opacity: 0 } to { opacity: 1 } }` gated like the other motion (off under `html[data-anim="off"]` and reduced motion, where it should simply show). Server-rendered skeletons need the delay in CSS because they paint before any script runs. Slice B then adds the minimum-show-time hook to `ui/Skeleton.tsx` and applies both to the core routes.
- Slice I (`src/app/globals.css:1193`): `.tv-frame` still carries a `box-shadow` (`0 10px 30px -18px`) in dark schemes, which the no-shadow rule retired. Suggested: drop the rule, or keep only the `0 0 0 1px var(--edge)` ring as a border.
- Slice E (`src/components/SiteFooter.tsx`), F166: accept an optional `aside` slot (rendered at the end of the bottom line) and a `socials` flag that renders `SocialsRow variant="quiet"`, so the home page can use the shared footer with its mod-only build stamp and drop its local copy.
- Slice H (`src/lib/multiplayer.ts`): export `ACTIVE_GAME_KEY` and `ACTIVE_GAME_TTL_MS`; the home page's pre-paint rejoin stamp mirrors them as literals today (commented at the site).
- Slice A (`src/components/SiteHeader.tsx`): at 360px the brand block measured wider than its box (`div.flex.min-w-0` client 154px, scroll 195px) on /play; af0c696 may already cover it.
- Slice A, P-A-guest-flag: yes please, if the tour flag joins the cookie seed the /play inline stamp can be removed.
- Slice D (`src/lib/presence.ts` is unowned; whoever takes it): it runs a second refcounted poll of the same `/api/lobby` snapshot. Suggested patch: replace its `poll`/`startPolling`/`stopPolling` with `subscribeLobbySnapshot(POLL_MS, (lobby) => { snapshot = lobby; emit(); })` from `lobbyClient.ts`, keeping its subscriber count to call the returned unsubscribe when the last listener leaves.
- Button owner (slice I, `src/components/ui/Button.tsx` / `.btn-leaf` in globals.css): the primary button's white label on the accent fill measures 3.27:1 (white on rgb(54,146,231), 13px), under AA; axe flags it on /play (Start game) and /lobby (the selected mode segment and Find). It is the brand accent, so it may be an owner call (a darker fill in the dark scheme would clear it).
- Slice I (`src/app/globals.css`, light scheme tokens): `--accent-buff-hi-rgb` (58 127 187) and `--accent-nerf-hi-rgb` (209 88 82) used as text measure 3.94:1 and 3.72:1 on the light raised surface (#f7f6f5) and 4.26 / 4.02 on white; axe flags 4 nodes on / (the hero's Buff and Nerf words, the ratings chips) and there are 40 `text-mode-*Glow` uses. Suggested: in `html[data-light]` set the two hi tokens to the base values (43 106 160 measures 5.30:1 and 192 65 59 measures 4.79:1 on #f7f6f5).
- Slice J or the owner of `src/components/BoardPlayerRow.tsx` and `src/components/MoveList.tsx`: on a 360px touch screen the player-name links are 18px tall and the compact move rows 23px, under the 44px touch floor.
- Integrator (harness, `scripts/polish/seed.ts`): the seeded `.polish-auth/*.json` states carry `dc_session` but no `nc_who` display cookie, so every signed-in cell in test:cls and test:console measures a first visit without the hint (the header chip and the lobby mode segments start as for a signed-out visitor). A returning player has the cookie after one /me. Suggested: after seeding, load `/api/auth/me` once in the context and save the storage state with the cookie.
- Integrator (`scripts/polish/cls-thresholds.json`): after wave 1 the /lobby cells measure 0.0012 / 0.0076 / 0.0064 / 0.0108 against ceilings 0.0174 / 0.022 / 0.005 / 0.019; ratchet with `test:cls --routes /,/lobby,/play --runs 2 --update` once the dev server holds still for a full run.


## TODO (left for wave 2)

- F026: the delayed skeleton primitive (150ms show delay, minimum show time) waits on slice I's CSS delay utility (REQUESTS); then a small client hook in `ui/Skeleton.tsx` and its use on the core routes.
- F166: waits on slice E's footer `aside` slot and `socials` flag (REQUESTS).
- F112: React Profiler before/after render counts for the bot game and the spectator board.
- /game/[id] touch targets: the meta-row player names (`HeaderIdentity`, 20px tall on touch); a hit-area fix has to avoid overlapping the compact nav just above it.
- Interaction passes (hover, focus, active, disabled, double submit) on /, /play, /game and /game/[id].
- test:cls on / and /play with --runs 2 on a stable server, and test:console on the bot game. This wave the shared dev server was restarted by its memory cap every 1 to 2 minutes, so several runs had to be repeated.
