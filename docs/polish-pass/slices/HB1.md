# Slice HB1 (house-bot policy, `src/lib/server/bots.ts`), wave 1

Owner: HB1 agent. Ledger updates for the integrator to fold into `LEDGER.md` and into the RESULTS table of `docs/polish-pass/slices/HB.md`.
Evidence: `docs/polish-pass/evidence/HB/HB1/`. Plan and acceptance lines: `docs/polish-pass/slices/HB.md`, track HB1.

## How it was measured

- Before: the unmodified tree at bbe4271 (`bots.ts` and `ai.ts` are identical at 9934d3e), exported to the scratchpad, running the same committed scripts. Files `before-*`.
- After: HEAD 9934d3e exported to the scratchpad with only the HB1 files laid over it, so the numbers are HB1's alone. HB2 is editing `src/engine/ai.ts` in the shared tree at the same time, and a card run taken in the shared tree moved (207 fires, 63.7%) because of it; the clean-tree run reproduces the earlier HB1-only run exactly (164 fires, 53.1%). Files `after-*`.
- Same seeds, position sets (`check-positions.json`, `blunder-positions.json`) and sample sizes before and after. Every command ran through `scripts/polish/heavy.sh nice -n 10`.
- The engine is not deterministic where a search is bounded by wall time (the snap reference search, `sim-house-bots`, the policy test's card harvest), so those counts move a little between runs on a loaded box. The ladder, check, blunder, variety and card runs freeze `Date.now` or use a fixed judge and repeat exactly.

## ROW UPDATES

| Row | Status | Evidence | Commit | Note |
|---|---|---|---|---|
| HB P1 (beginner tiers give their king away) | DONE | `before-check.txt` / `after-check.txt`, `before-ladder.txt` / `after-ladder.txt` | e5cab80 (scripts 69eda01) | King left capturable after a move played in check, 30 positions x 10 picks (n=300 each): 800 57.0% to 2.0% (CI 0.9-4.3), 900 53.7% to 1.0%, 1050 47.3% to 0.0%, 1200 10.3% to 0.0%, 1350 5.0% to 0, 1450 3.0% to 0, 1550 and 1650 1.3% to 0 (targets 8 / 6 / 4 / 3 / 1%). Cause: level `easy` returned a noisy one-ply pick before reading the baked depth, top-K, temperature and noise, and added its own 22% `Math.random` move. 800-1050 now run `medium` sampled at depth 1 (1050's depth-2 cost failed the DO node guard, see below); 1050 re-tuned to top-K 4, 150cp, 70cp. A king-safety floor covers every house path; the 800 and 900 check leak (7% and 4.5%) is the only exception, by design. Ladder (Nerf, 20 games a pair, frozen clock): 900 v 800 55.0% to 60.0%, 1050 v 900 40.0% to 70.0%, 1200 v 1050 75.0% to 77.5%, 1350 v 1200 87.5% to 87.5%, 1450 v 1350 50.0% to 55.0%, 1200 v 800 82.5% to 87.5%; every step is now 55% or more and monotonic (before, 1050 lost to 900). CI is about +-20 points at n=20, so this shows direction, not calibration. The before ladder is not repeatable run to run, because the old `easy` path drew `Math.random` (an earlier before run read 50.0 / 52.5 / 87.5 / 87.5 / 50.0 / 70.0); the after ladder repeats exactly. Median game length for 800 v 900 went from about 17 to 44 moves a side. DO cost guard (A25): estimated nodes per move p95 2.8k / 3.2k / 3.2k for 800 / 900 / 1050 (target 10k), max about 26k, under the 48-80k node caps; the sampled path does not report nodes, so the estimate is CPU time x the box's argmax node rate measured in the same run (208 nodes/ms). Before: p95 under 60 nodes (the one-ply greedy path). |
| HB P3 (a blunder is a uniformly random move) | DONE | `before-blunder.txt` / `after-blunder.txt` | e5cab80 | Forced blunders on 40 midgame positions, 2 per tier, judged by hard@1000 (n=80 a tier, 320 pooled): king hangs 7 to 0; pooled median loss 240 to 249cp, and now it depends on the tier (800 330, 1200 278, 1550 108, 1900 120); share at 900cp or more 12.8% to 0.0% (target 10% or less). A blunder is a king-safe, nerf-safe move that leaves something to take (the opponent's best capture is worth the window's floor) and loses 100-600cp statically at 800-1050, 100-450 to 1450, 80-350 above; if none fits, a depth-1 top-4 sample. Found on the way: a 0.5 cap on the pressure-scaled blunder chance halved any profile above 0.5, so a probe forcing 1 got blunders on only half its moves; the cap now never lowers a profile's own chance. |
| HB P2 and P16 (clock cliff, metronome thinks) | DONE (bots.ts side); worker wiring is R1 and R2 | `before-clock.txt` / `after-clock.txt`, `after-sim-house-bots.txt` | e5cab80 | Model: 400 runs a cell, RTT 500ms, alarm 250-300ms, worker as it is today. 1+0 median flag move 34-41 to 72-81 (p10 32-37 to 70-78; target 50 / 40); 1+0 first 40 moves searched at 30ms or less 59-72% to 0% (target 10%); 3+0 median 62-92 to 105-141 (target 100); 2+1, 3+2 and 5+3 never flag inside 150 moves before or after. With R1 and R2 applied: 1+0 median 117-155 (target 70). Pacing with R2: own moves 1-5 under 1.2s 95-100% in every pool (before 9-18%); 6s or more 0 at 1+0 and 2+1 and 0 on forced moves. Filler: 10,000 draws, 0 mismatches against an embedded copy of the old function, with and without the new options. `sim-house-bots.ts` pacing assertions rewritten to the new spec in the same commit (the old spec asserted the flat 1-3s think under 3 minutes). |
| HB P15 (snap forced branch dead, snapped moves pay the round trip) | DONE (bots.ts side); worker wiring is R11 | `before-snap.txt` / `after-snap.txt`, `before-snap-oldtest.txt` | e5cab80 | Forced branch fires on 0 of 20 one-king-safe-move positions before, 20 of 20 after (`snapContext(g, me, { kingSafe: true })`; the option is opt-in so the arena's filler call is unchanged, asserted). `houseSnapMove`: on 50 positions where hard@400 recaptures, it plays that move 45-46 times (90-92%, the count moves with box load because the reference search is timed), declines 3; answering anywhere it differs from hard@400 3-4 times in 49; never king-unsafe. The test's definition of a recapture position was tightened from "a recapture exists" (where declining was scored as wrong even when the search itself did not recapture) to "the reference search recaptures there", plus a separate wrong-answer cap. |
| HB P5 (no variety, styled personas lose a ply) | DONE (bots.ts side); engine-service uses `varietySeed` (HB3) and R3 | `before-variety.txt` / `after-variety.txt` | e5cab80 | No persona, 20 seeds, 8 plies: distinct sequences 7 / 4 / 1 / 1 to 20 / 19 / 18 / 18 at 1350 / 1650 / 2000 / 2400 (target 8). 40 personas as White: 9 distinct first moves to 8 (target 8; the one lost is a d2d3 the search used to pick, now the book's). Styled argmax personas on the sampling path: 53/57, 69/70, 20/21, 43/45 to 0 at every tier; mean depth styled equals baked (3.00 / 4.00 / 4.00 / 4.00; not reported on the old sampling path). Repertoire: about 50 mainstream lines, weighted per persona, followed 4-12 plies by tier, only when the position is exactly the book's and the move is king-safe and nerf-safe; `houseBookIsLegal` is asserted. |
| HB P11 and P17 (cards and drafts) | DONE (bots.ts side); worker wiring is R9, arena is HB3 | `before-cards.txt` / `after-cards.txt`, `after-policy.txt` | e5cab80 | 80 paired Buff-mode games (40 seeds, colors swapped) at 1350-1650. Bad fires (turn-costing card with an own piece of 3+ hanging that a king-safe move would have saved): today's policy 14-19 per about 100, `houseChooseActivation` 0. Paired score 53.1% (CI 42.3-63.7) against today's policy: no worse, not proven better. Unused activated cards per seat 0.44 against today's 0.30 (baseline today v today 0.28 / 0.46); of those, cards the engine's own gates had accepted at some point 0.17 against 0.10. The 0.2 target is met for gate-accepted cards only; see PARTIAL below. Drafts: personas disagree on the pick in 0 of 200 same-tier offers before, 177 (88.5%, CI 83.3-92.2) after (target 30%); bank rate by offered tier 12% flat before, 11.9% at tier 1 falling to 0% at tier 8 after. The sim's bad-fire metric was refined to count only fires a move could have avoided, which matches the goal text ("and a move saves it"); the before numbers were re-run with it. |
| HB P8 and P13 (resign, draw, rematch, breaker, timeout) | DONE (pure helpers with tests); worker wiring is R5, R6, R7 | `after-policy.txt` | e5cab80 | `test-house-policy.ts`: existing `houseStyle` traits 0 changed values across 900 personas (new traits use new salts); 1500+ resign within 6 own moves in 98 of 100 lost histories; 800-1200 median 32 own moves; 0 resigns on level histories; never-resign personas (3 by bio or name, 62 in all) never resign; draws 20 of 20 dead-level accepted, 0 of 20 winning; rematch 65.7-68.5% by band with 2-6s delays; breaker transitions (closed, open after 2 failures, one half-open probe after 45s, closed on success, never opened by 409 or a rejected move); `houseEngineTimeoutMs` is budget + 1s capped at 3s. Before: none of these exist (the test fails to import on bbe4271). |
| HB P22 (doc describes the 80ms bot, missing weakening spec) | PARTIAL | `before-audit.txt` / `after-audit.txt` | 4a6226d | `audit-house-bots.ts` now fails when the strength table in `docs/house-bots.md` differs from `HOUSE_SKILL_PROFILES` (fails on the old doc, passes now). Strength, pacing, cards, behaviour helpers, the remote engine and a Moderator overrides section (replacing the never-committed `docs/bot-weakening-spec.md`) are rewritten; `bots.ts` and `sim-house-bots.ts` point there. Left: HB2's engine changes and HB3's service changes, once their RESULTS land; the references in files HB1 does not own (REQUEST below). |
| HB P12 (compressed ladder, re-space 1350-2700 budgets) | TODO | none | none | Waits for HB2's final depth-per-budget table (goal 7). HB2 is mid-edit in `ai.ts`; re-spacing against today's engine would be thrown away. |

## Acceptance lines not fully met

- Cards, unused activated cards per seat 0.2 or fewer: 0.44 overall, 0.17 for cards the gates ever accepted. The rest are cards `aiChooseBuffActivation` never accepts in these games (no house policy can fire them without loosening the engine's gates in `game.ts`), and the new drafting lean holds slightly more activated cards. Raising the appetite for a card that does as well as the best move to 0.65 moved the overall figure only from 0.47 to 0.44.
- Everything else in the HB1 acceptance list is met except goal 7 (TODO above) and the HB2/HB3 parts of goal 8.

## ADDITIONS

- `sim-house-ladder.ts` reports an estimated node count for the sampled path (CPU time x a same-run argmax calibration), since the engine's sampled path does not fill `SearchStats.nodes`. HB2 may want to report nodes from `rankedRoot` too.
- `sim-house-cards.ts` reports how many unused cards the engine's gates had ever accepted, and env `HB_DEBUG_BAD=1` / `HB_DEBUG_UNUSED=1` print each bad fire and each seat's leftover cards.
- Seen once on a heavily loaded box (load 4+ with niced jobs): `pickAIMove(hard, 80)` returning after 500-690ms in a few positions with 88-141 root moves; the same positions at load 2 stay under 90ms. Not reproducible, noted for HB2.

## PROPOSALS

None.

## OWNER QUESTIONS

None.

## REQUESTS

- To slice H or the integrator (`worker.ts`): R1, R2, R5, R6 (with the material fallback `houseMaterialEvalCp` until R3), R7, R9 and R11 in `docs/polish-pass/slices/HB.md` are unblocked now that e5cab80 has landed the HB1 API: `houseMoveBudgetMs(budget, remaining, ceiling, incrementSec)`, `houseThinkMs(random, clock, timeSec, mult, tempo, { incrementSec, expectedSearchMs, ownMoveIndex, kingSafeMoves, capturesAvailable })`, `houseExpectedSearchMs`, `houseEngineTimeoutMs`, `HouseEngineBreaker`, `snapContext(game, me, { kingSafe: true })`, `houseSnapMove`, `houseChooseActivation`, `houseDraftChoice`, `houseResignDecision`, `houseDrawDecision`, `houseDrawOffer`, `houseRematchDecision`, `pickHouseMove(..., persona, { varietySeed, incrementSec, stats })`. Filler callers must keep calling `houseThinkMs` with the multiplier and `snapContext` without the option.
- To HB2 (`src/engine/ai.ts:304`): the comment cites `docs/bot-weakening-spec.md`, which does not exist; point it at `docs/house-bots.md`, "Moderator overrides".
- To the owners of `src/lib/server/settings.ts:28` and `src/app/api/mod/house/route.ts:59`: same replacement, `docs/bot-weakening-spec.md` to `docs/house-bots.md` ("Moderator overrides"). Comment-only.
- To HB3: `pickHouseMove`'s eighth argument takes `{ varietySeed }`; the engine service can pass hash(match seed, side to move) when no persona is sent.
- To the integrator: fold the rows above into HB.md RESULTS.

## Commits

- 69eda01 measurement scripts and before evidence
- e5cab80 the bot policy in `bots.ts`, the rewritten `sim-house-bots` pacing checks, after evidence
- 4a6226d `docs/house-bots.md` and the audit's doc table check
