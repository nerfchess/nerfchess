node_modules
.next
.next-wp
.next-fx
.open-next
.wrangler
.wrangler-dry-run
.codex-run/
.omc/
out
.env*
*.log
.DS_Store
next-env.d.ts
*.tsbuildinfo
agent*.jsonl
.vscode/
dist-server

# Linting files
.eslintcache

# Stray scratch / stderr-redirect artifacts
err.txt
tree.json

# Claude: local agent state and instruction files stay private, but the
# shared SessionStart hook (.claude/settings.json and .claude/hooks/) is
# committed so a cloud session installs dependencies on its own (F252).
.claude/*
!.claude/settings.json
!.claude/hooks/
CLAUDE.md

# Build outputs for OCI sidecar services (regenerated by build.mjs)
dist/

# Playwright artifacts
/test-results/
/playwright-report/
# Route-sweep output: a full-page screenshot per route x width x theme cell
# plus the JSON defect list. Regenerated by `playwright test e2e/sweep.spec.ts`
# and far too large (and too machine-specific) to commit.
/e2e/__screens__/

# Written by `next dev` on every start, not authored.
AGENTS.md

# Win-rate sweep shard partials. These are RESUMPTION STATE, not results: each
# is rewritten every 10 cards so a killed run continues instead of restarting,
# so a committed snapshot would be a fraction of a sweep that reads like a
# finished one. The merged result is committed on completion.
docs/card-winrate.shard*.json


# Polish-pass harness: seeded local session cookies (scripts/polish/seed.ts).
/.polish-auth/
