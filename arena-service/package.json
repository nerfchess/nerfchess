{
  "name": "nerfchess-arena-service",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Tier 2 · M1 — runs bot-vs-bot house games in RAM, isolated from the DO.",
  "scripts": {
    "build": "node build.mjs",
    "start": "node dist/server.mjs"
  },
  "dependencies": {
    "ws": "^8.21.0"
  },
  "devDependencies": {
    "@types/ws": "^8.18.1",
    "esbuild": "^0.24.0"
  }
}
