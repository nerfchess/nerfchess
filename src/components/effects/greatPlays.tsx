// Tier 5-6 plugin signatures ("great" tier: unique, really cool plays one
// band below the god-tier set). See sigPlugins.tsx for the contract.
// Self-contained: own SVG, own CSS (greatPlays.css), transform/opacity only.
// Do NOT import from BoardEffects.tsx.

import type { SigPlugin } from "./sigPlugins";

export const PLAYS: Record<string, SigPlugin> = {
  // Filled by the tier 5-6 spectacle workstream.
};
