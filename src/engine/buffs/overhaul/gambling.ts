// Overhaul gambling set (cards 201-224 of docs/overhaul-roster.md): 3 per
// tier. Server-seeded RNG only (api.rng), displayed odds must match code
// constants, board-based payouts. Ids gm_*.

import type { Buff } from "./shared";

export const OVERHAUL_GAMBLING: Buff[] = [];
